fn test_double(int x) -> float:
    if x:
    	return 0.0
    else:
    	if x:
    		return 0.0
    return x/1.0


fn test_int(int x) -> int:
	if x <= 1:
		return 1
	else:
		return test_int(x+1, 2) + test_int(x-1,)

